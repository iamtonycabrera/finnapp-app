import 'package:flutter/material.dart';

class DisableInternetPage extends StatelessWidget {
  const DisableInternetPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
